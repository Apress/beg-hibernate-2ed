
    alter table Product 
        drop constraint FK50C664CF8DF2A0BB;

    alter table Software 
        drop constraint FK527DC5C7AFBDC331;

    drop table Product if exists;

    drop table Software if exists;

    drop table Supplier if exists;

    create table Product (
        id integer generated by default as identity (start with 1),
        name varchar(255),
        description varchar(255),
        price double,
        supplierId integer,
        primary key (id)
    );

    create table Software (
        productId integer not null,
        version varchar(255),
        primary key (productId)
    );

    create table Supplier (
        id integer generated by default as identity (start with 1),
        name varchar(255),
        primary key (id)
    );

    alter table Product 
        add constraint FK50C664CF8DF2A0BB 
        foreign key (supplierId) 
        references Supplier;

    alter table Software 
        add constraint FK527DC5C7AFBDC331 
        foreign key (productId) 
        references Product;
