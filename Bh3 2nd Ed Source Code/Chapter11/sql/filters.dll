
    drop table User if exists;

    create table User (
        id integer generated by default as identity (start with 1),
        username varchar(32),
        activated bit,
        primary key (id)
    );
