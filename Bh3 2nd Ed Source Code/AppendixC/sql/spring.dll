
    alter table Paper_Article 
        drop constraint FKC2E401032E0E7DB2;

    alter table Paper_Article 
        drop constraint FKC2E4010327FE1859;

    drop table Article if exists;

    drop table Paper if exists;

    drop table Paper_Article if exists;

    create table Article (
        id integer generated by default as identity (start with 1),
        headline varchar(255),
        byline varchar(255),
        content varchar(255),
        primary key (id)
    );

    create table Paper (
        id integer generated by default as identity (start with 1),
        title varchar(255),
        primary key (id)
    );

    create table Paper_Article (
        Paper_id integer not null,
        articles_id integer not null,
        primary key (Paper_id, articles_id),
        unique (articles_id)
    );

    alter table Paper_Article 
        add constraint FKC2E401032E0E7DB2 
        foreign key (articles_id) 
        references Article;

    alter table Paper_Article 
        add constraint FKC2E4010327FE1859 
        foreign key (Paper_id) 
        references Paper;
